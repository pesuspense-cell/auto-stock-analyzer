# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environment
.venv/
venv/
env/

# Streamlit secrets (API 키 포함 — 절대 커밋 금지)
.streamlit/secrets.toml

# 개인 데이터 및 API 키 (절대 커밋 금지)
watchlist.json
settings.json

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# 환경 변수 (자격증명 포함 — 절대 커밋 금지)
.env

# 로컬 DB 캐시 (사용자 데이터 포함 — 절대 커밋 금지)
*.db

# 종목 메타데이터 캐시 (7일 TTL, 로컬 자동 재생성)
stock_metadata.json

# 시그널 봇 런타임 상태 (고점가·트레일링·중복발송 방지 키, 로컬 자동 재생성)
signal_bot_state.json

# 텔레그램 연동 리스너 getUpdates 오프셋 (로컬 자동 재생성)
.telegram_offset

# 로그
logs/

# Redis 포터블 바이너리
redis/

# Claude 내부 파일
.claude/
settings.json.migrated

# 백테스팅 결과 (로컬 자동 생성)
backtest_results/

# ETF 캐시 (로컬 자동 재생성)
data/etf_fundamental_cache.json

# 임시 스크린샷
*.png
